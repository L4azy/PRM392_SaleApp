```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Docker Deployment Architecture                      │
└─────────────────────────────────────────────────────────────────────────────┘

                                External Access
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
              Port 8080           Port 1433         Port 8080/swagger-ui
                    │                  │                  │
                    ▼                  ▼                  ▼
         ┌──────────────────────────────────────────────────────┐
         │            Docker Compose Network                     │
         │              (salesapp-network)                       │
         │                                                       │
         │  ┌─────────────────────┐    ┌─────────────────────┐ │
         │  │                     │    │                     │ │
         │  │   Spring Boot App   │◄───┤   SQL Server 2022   │ │
         │  │   (salesapp-        │    │   (salesapp-        │ │
         │  │    backend)         │    │    sqlserver)       │ │
         │  │                     │    │                     │ │
         │  │  - Java 17          │    │  - Database:        │ │
         │  │  - Port 8080        │    │    SalesAppDB       │ │
         │  │  - Spring Boot 3.2  │    │  - Port 1433        │ │
         │  │  - REST API         │    │  - sa/password      │ │
         │  │  - WebSocket        │    │  - Auto Init        │ │
         │  │                     │    │                     │ │
         │  └─────────────────────┘    └─────────────────────┘ │
         │           │                          │               │
         │           │                          │               │
         │           ▼                          ▼               │
         │  ┌─────────────────────┐    ┌─────────────────────┐ │
         │  │  Environment        │    │  Persistent         │ │
         │  │  Variables          │    │  Volume             │ │
         │  │  (.env)             │    │  (sqlserver-data)   │ │
         │  │                     │    │                     │ │
         │  │  - DB Credentials   │    │  Database Files     │ │
         │  │  - JWT Key          │    │  Transaction Logs   │ │
         │  │  - API Keys         │    │  Backup Files       │ │
         │  └─────────────────────┘    └─────────────────────┘ │
         │                                                       │
         └───────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         Deployment Flow Diagram                             │
└─────────────────────────────────────────────────────────────────────────────┘

    1. docker-compose up --build
           │
           ▼
    2. Build Spring Boot App
       ┌──────────────────────┐
       │ Maven Build          │
       │ - Download deps      │
       │ - Compile code       │
       │ - Package JAR        │
       └──────────────────────┘
           │
           ▼
    3. Start SQL Server
       ┌──────────────────────┐
       │ SQL Server starts    │
       │ Health check         │
       │ Wait for ready       │
       └──────────────────────┘
           │
           ▼
    4. Initialize Database
       ┌──────────────────────┐
       │ Create SalesAppDB    │
       │ Run init scripts     │
       │ Create tables        │
       └──────────────────────┘
           │
           ▼
    5. Start Spring Boot App
       ┌──────────────────────┐
       │ Connect to DB        │
       │ Initialize Hibernate │
       │ Start web server     │
       └──────────────────────┘
           │
           ▼
    6. Ready! 🚀
       - API: localhost:8080
       - Swagger: localhost:8080/swagger-ui.html
       - DB: localhost:1433


┌─────────────────────────────────────────────────────────────────────────────┐
│                         File Structure Overview                             │
└─────────────────────────────────────────────────────────────────────────────┘

PRM392_SaleApp/
│
├── 📋 docker-compose.yml          ← Main orchestration file
├── 📋 .env.example                ← Environment template
├── 📋 .env                        ← Your credentials (not in git)
├── 📋 .gitignore                  ← Git ignore rules
│
├── 📁 init-db/                    ← Database initialization
│   ├── 01-init-database.sql      ← SQL Server schema
│   └── entrypoint.sh             ← Init helper script
│
├── 📁 SalesApp/                   ← Your application
│   ├── 📋 Dockerfile              ← App container definition
│   ├── 📋 .dockerignore           ← Build exclusions
│   ├── 📋 pom.xml                 ← Maven (with MSSQL driver)
│   └── 📁 src/                    ← Source code
│       ├── main/
│       │   ├── java/
│       │   └── resources/
│       │       └── application.yaml  ← Spring config
│       └── test/
│
├── 📜 DOCKER_DEPLOYMENT.md        ← Full deployment guide
├── 📜 DOCKER_SETUP_SUMMARY.md     ← This overview
├── 📜 README.md                   ← Main readme
│
├── 🚀 start-docker.bat            ← Windows CMD launcher
└── 🚀 start-docker.ps1            ← PowerShell launcher


┌─────────────────────────────────────────────────────────────────────────────┐
│                         Network Communication                               │
└─────────────────────────────────────────────────────────────────────────────┘

External Client                      Docker Network
     │                                    │
     │ HTTP Request                       │
     ▼                                    │
┌─────────┐                               │
│ Browser │                               │
│  :8080  │                               │
└─────────┘                               │
     │                                    │
     └────────────────────►┌──────────────┴──────────────┐
                           │  Spring Boot Container      │
                           │  salesapp-backend:8080      │
                           └──────────────┬──────────────┘
                                         │
                                         │ JDBC Connection
                                         │ jdbc:sqlserver://sqlserver:1433
                                         │
                                         ▼
                           ┌─────────────────────────────┐
                           │  SQL Server Container       │
                           │  salesapp-sqlserver:1433    │
                           │  Database: SalesAppDB       │
                           └─────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                         Key Technologies                                     │
└─────────────────────────────────────────────────────────────────────────────┘

Backend Framework:  Spring Boot 3.2.1
Language:          Java 17
Build Tool:        Maven 3.9.6
Database:          Microsoft SQL Server 2022
ORM:               Hibernate (JPA)
Security:          Spring Security + JWT
API Docs:          OpenAPI 3 (Swagger)
WebSocket:         Spring WebSocket
Mapping:           MapStruct
Utilities:         Lombok
Image Storage:     Cloudinary
AI Integration:    Google Gemini
Containerization:  Docker + Docker Compose
```
